- [x] `EditorRow` の `_delete` ボタンの挙動を `_toggle_delete` に変更し、無効化・グレーアウトを実装する。
- [x] 「削除」と「元に戻す」のトグル動作を確認する。
- [x] ダイアログ内に `on_closing` ハンドラーを追加し、未保存時の警告ダイアログを実装する。
- [x] 「適用」ボタン押下時に、未保存状態フラグ（`initial_editors`）がリセットされることを確認する。
